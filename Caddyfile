# Caddy Internal — Homelab reverse proxy
# Served via Tailscale Services (caddy-lab-01 + caddy-lab-02)

# Health check endpoint
:8080 {
	respond "caddy-internal healthy" 200
}

paperless.meltforce.net {
	tls {
		dns cloudflare {env.CLOUDFLARE_API_TOKEN}
	}
	reverse_proxy 100.80.158.113:8000
}
